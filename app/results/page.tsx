import Nav from '@/components/Nav';import Link from 'next/link';import {createClient} from '@/lib/supabase/server';export default async function Results({searchParams}:{searchParams:Promise<{id?:string}>}){const {id}=await searchParams;const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return null;const {data:a}=await s.from('exam_attempts').select('*').eq('id',id??'').eq('user_id',user.id).single();return <div className="shell"><Nav/><main className="container"><div className="card"><div className="eyebrow">RESULT</div><h1>Mock completed</h1>{a?<><div className="stats"><div className="stat"><span>Score</span><b>{a.score}</b></div><div className="stat"><span>Correct</span><b>{a.correct_count}</b></div><div className="stat"><span>Wrong</span><b>{a.wrong_count}</b></div><div className="stat"><span>Unanswered</span><b>{a.unanswered_count}</b></div></div><p className="notice">Scoring: +3 correct, −1 wrong, 0 unattempted.</p></>:<p className="error">Result not found.</p>}<div style={{display:'flex',gap:10,marginTop:18}}><Link href="/analytics" className="btn secondary">View analytics</Link><Link href="/mock" className="btn ghost">Take another mock</Link></div></div></main></div>}
