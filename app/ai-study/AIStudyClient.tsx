'use client';
import {useState} from 'react';
export default function AIStudyClient(){const [topic,setTopic]=useState('Operating Systems');const [difficulty,setDifficulty]=useState('Medium');const [count,setCount]=useState(3);const [items,setItems]=useState<any[]>([]);const [busy,setBusy]=useState(false);const [error,setError]=useState('');async function generate(){setBusy(true);setError('');try{const r=await fetch('/api/ai/generate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({topic,difficulty,count})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Generation failed');setItems(d.questions||[])}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <main className="container"><div className="hero"><div className="card"><div className="eyebrow">AI STUDY ASSISTANT</div><h1>Target your weak points.</h1><p className="muted">Generate original practice questions from your chosen C-CAT topic. AI-generated content is clearly labeled and is not official C-CAT material.</p><div className="grid2"><label>Topic<select className="input" value={topic} onChange={e=>setTopic(e.target.value)}><option>Operating Systems</option><option>C Programming</option><option>Data Structures</option><option>Networking</option><option>Quantitative Aptitude</option><option>Reasoning</option><option>English</option><option>Big Data & AI</option><option>Computer Architecture</option></select></label><label>Difficulty<select className="input" value={difficulty} onChange={e=>setDifficulty(e.target.value)}><option>Easy</option><option>Medium</option><option>Hard</option></select></label></div><label>Questions<select className="input" value={count} onChange={e=>setCount(Number(e.target.value))}><option value={1}>1</option><option value={3}>3</option><option value={5}>5</option></select></label><button className="btn primary" disabled={busy} onClick={generate}>{busy?'Generating…':'Generate adaptive practice'}</button>{error&&<div className="notice error" style={{marginTop:12}}>{error}</div>}</div><div className="card"><h2>How it works</h2><div className="list"><div className="row">📊 Uses your recent performance signals</div><div className="row">🧠 Generates original topic-focused questions</div><div className="row">🔒 API key stays server-side</div><div className="row">⚠️ Generated questions remain separate from the official question bank</div></div></div></div>{items.length>0&&<section><div className="section-title"><div><h2>Generated Practice Set</h2><p className="muted">AI-generated • verify concepts before relying on them for high-stakes preparation.</p></div></div><div className="grid">{items.map((x:any,i)=><article className="card" key={x.id||i}><div className="toolbar"><span className="pill">AI Generated</span><span className="pill">{x.source_topic}</span><span className="pill">{x.difficulty}</span></div><h3>{i+1}. {x.question}</h3><div className="options">{(x.options||[]).map((o:string,n:number)=><div className="option" key={o}><span className="option-letter">{String.fromCharCode(65+n)}</span>{o}</div>)}</div><div className="notice"><b>Answer:</b> {String.fromCharCode(65+x.correct_answer)}<p>{x.explanation}</p></div></article>)}</div></section>}</main>}
