import Link from 'next/link';import Nav from '@/components/Nav';import {createClient} from '@/lib/supabase/server';
export default async function Dashboard(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return null;const {count}=await s.from('questions').select('*',{count:'exact',head:true});const {data:attempts}=await s.from('exam_attempts').select('score,created_at,mode').eq('user_id',user.id).order('created_at',{ascending:false}).limit(5);return <div className="shell"><Nav/><main className="container"><section className="hero"><div className="card"><div className="eyebrow">C-CAT PREPARATION • V5</div><h1>Train like the real exam.</h1><p className="muted">Next.js + Supabase foundation with authenticated users, PostgreSQL questions, attempts and analytics.</p><div style={{display:'flex',gap:10,flexWrap:'wrap'}}><Link className="btn primary" href="/mock">Start 2-Hour Mock</Link><Link className="btn ghost" href="/practice">Practice Questions</Link></div></div><div className="card"><h2>Exam structure</h2><p>Section A — 50 Q • 60 min • 150 marks</p><p>Section B — 50 Q • 60 min • 150 marks</p><p className="notice">+3 correct • −1 wrong • 0 unattempted</p></div></section><div className="stats"><div className="stat"><span>Question bank</span><b>{count??0}</b></div><div className="stat"><span>Mocks completed</span><b>{attempts?.length??0}</b></div><div className="stat"><span>Latest score</span><b>{attempts?.[0]?.score??0}</b></div><div className="stat"><span>Account</span><b>✓</b></div></div><div className="section-title"><h2>Recent attempts</h2><Link href="/analytics">View analytics →</Link></div><div className="card list">{attempts?.length?attempts.map(a=><div className="row" key={a.created_at}><span>{a.mode}</span><b>{a.score} marks</b></div>):<p className="muted">No attempts yet. Start your first mock.</p>}</div></main></div>}
