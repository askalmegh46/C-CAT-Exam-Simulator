import Nav from '@/components/Nav';import {createClient} from '@/lib/supabase/server';export default async function AdminQuestions(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return null;const {data:qs}=await s.from('questions').select('id,section,topic,difficulty,source_type,source_label,question').order('id').limit(100);return <div className="shell"><Nav/><main className="container"><div className="section-title"><div><div className="eyebrow">ADMIN FOUNDATION</div><h1>Question Manager</h1></div><span className="pill">{qs?.length??0} questions</span></div><div className="card"><p className="notice">This first Supabase version focuses on database-backed browsing. Add/edit/delete actions can be enabled with an admin role and RLS policies in the next stage.</p></div><div className="card" style={{marginTop:18}}><table className="table"><thead><tr><th>Section</th><th>Topic</th><th>Difficulty</th><th>Question</th><th>Source</th></tr></thead><tbody>{qs?.map(q=><tr key={q.id}><td>{q.section}</td><td>{q.topic}</td><td>{q.difficulty}</td><td>{q.question}</td><td>{q.source_type}</td></tr>)}</tbody></table></div></main></div>}
