import Nav from '@/components/Nav';
import {createClient} from '@/lib/supabase/server';
export default async function Leaderboard(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return null;const [{data:leaders},{data:avg}]=await Promise.all([s.rpc('get_leaderboard',{limit_count:50}),s.rpc('get_global_average')]);return <div className="shell"><Nav/><main className="container"><div className="section-title"><div><div className="eyebrow">COMPETITION</div><h1>Global Leaderboard</h1><p className="muted">Anonymous-friendly rankings based on completed C-CAT simulator attempts.</p></div><div className="stat"><span>Global average</span><b>{Number(avg||0)}</b></div></div><div className="card"><table className="table"><thead><tr><th>Rank</th><th>Student</th><th>Score</th><th>Mode</th><th>Date</th></tr></thead><tbody>{(leaders||[]).map((x:any)=><tr key={`${x.rank}-${x.created_at}`}><td>#{x.rank}</td><td>{x.display_name}</td><td><b>{x.score}</b></td><td>{x.mode}</td><td>{new Date(x.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div></main></div>}
