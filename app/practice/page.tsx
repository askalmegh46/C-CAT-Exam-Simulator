import Nav from '@/components/Nav';import {createClient} from '@/lib/supabase/server';import PracticeClient from './PracticeClient';
export default async function Practice(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return null;const {data:questions}=await s.from('questions').select('id,section,topic,difficulty,question,options,correct_answer,explanation,source_type,source_label').order('section').order('id').limit(500);return <div className="shell"><Nav/><main className="container"><div className="section-title"><div><div className="eyebrow">QUESTION BANK</div><h1>Practice</h1></div><span className="pill">{questions?.length??0} loaded</span></div><PracticeClient questions={questions??[]}/></main></div>}
